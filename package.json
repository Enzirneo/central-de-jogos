{
  "name": "central-de-jogos",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/server",
    "packages/*",
    "packages/games/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "npm@10.9.2"
}
